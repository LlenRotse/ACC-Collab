# Python 3.10.  Install into a fresh virtualenv on a CUDA (>= 12.1) machine:
#     python3 -m venv venv
#     ./venv/bin/pip install -r requirements.txt
#
# The three versions that matter for reproducibility are pinned exactly
# (vllm, transformers, trl). vllm==0.6.1.post2 pins torch==2.4.0 and pulls in a
# newer `openai`/`httpx` than the originals below, so those two were relaxed
# (they only affect the optional GPT/OpenAI code path, not the local pipeline):
#     original: openai==1.3.5, httpx==0.23.0  ->  resolved by vllm to the versions below.

# --- core (pinned) ---
vllm==0.6.1.post2
transformers==4.44.2
trl==0.9.6
torch==2.4.0

# --- training / inference stack ---
peft==0.19.1
bitsandbytes==0.49.2
accelerate==0.34.2
datasets==5.0.0
sentencepiece==0.2.1
xformers==0.0.27.post2

# --- utilities used by the code ---
openai==2.44.0
httpx==0.28.1
tenacity==9.1.4
numpy==1.26.4
pandas==2.3.3
pyyaml==6.0.3
matplotlib==3.10.9
rich==15.0.0
wandb==0.26.1
